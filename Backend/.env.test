# tests/setup/env.test.example  (copy as .env.test at project root)
NODE_ENV=test
PORT=5050
APP_NAME=RK-Photo-Test
CLIENT_URL=http://localhost:3001
JWT_SECRET=tests-are-fun

# IMPORTANT: use a safe test DB (ideally local), not production!
# Example local:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rk_photo_test?sslmode=disable

# If you *must* use the Azure server, create a separate test database/user with minimal perms.

# External services are mocked in tests, but keep placeholders:
BREVO_SMTP_HOST=localhost
BREVO_SMTP_PORT=587
BREVO_SMTP_USER=test
BREVO_SMTP_PASS=test

AZURE_STORAGE_ACCOUNT_NAME=test
AZURE_STORAGE_ACCOUNT_KEY=test
AZURE_QR_CONTAINER_NAME=test
AZURE_PHOTO_CONTAINER_NAME=test

STRIPE_SECRET_KEY=sk_test_dummy

# Any other keys you reference in code can be dummy values here.
